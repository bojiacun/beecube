
function substr(str, len, repStr) {
    if(!str) return "";
    if (!repStr) repStr = "...";
    return str.substring(0, len) + (str.length > len ? repStr : '');
}


module.exports = {
    substr: substr,
}
