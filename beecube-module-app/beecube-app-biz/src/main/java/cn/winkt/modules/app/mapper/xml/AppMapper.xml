<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.winkt.modules.app.mapper.AppMapper">


    <select id="selectPageJoinAppModule" parameterType="Object" resultType="cn.winkt.modules.app.vo.AppDTO">
        select a.*, m.name as module_name, au.username as admin_username
        from beecube_apps a
                 left join beecube_modules m on m.id = a.module_id
                 left join beecube_app_users au on au.app_id = a.id
    </select>


</mapper>